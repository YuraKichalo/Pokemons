import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 25,
    paddingBottom: 40
  },
  scrollContainer: {
    flexGrow: 1
  },
  divider: {
    marginVertical: 30
  }
})
