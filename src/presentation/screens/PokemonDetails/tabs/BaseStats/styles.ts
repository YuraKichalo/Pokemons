import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 20
  },
  subtitle: {
    fontWeight: 'bold',
    fontSize: 16,
    marginBottom: 20,
    marginTop: 40
  }
})
